import HomeClient from '@/components/HomeClient';
import MillerzShowcaseServer from '@/components/MillerzShowcaseServer';

export default function Page() {
  return (
    <HomeClient>
      <MillerzShowcaseServer />
    </HomeClient>
  );
}
