'use client';

import React from 'react';
import MillerzShowcaseServer from './MillerzShowcaseServer';

export default function HomeClient() {
  return (
    <main className="min-h-screen">
      {/* Keep your other client-side homepage sections here. */}
      <MillerzShowcaseServer />
    </main>
  );
}
